parameters:
- name: skipTaskX
  type: boolean
  default: false
- name: skipTaskY
  type: boolean
  default: false

steps:
- script: echo "In Template"
- script: echo "Task X"
  condition: ${{ not(skipTaskX) }}
- script: echo "Task Y"
  condition: ${{ not(skipTaskY) }}
