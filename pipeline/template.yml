parameters:
- name: skipTaskX
  type: boolean
  value: false
- name: skipTaskY
  type: boolean
  value: false

steps:
- script: echo "In Template"
- script: echo "Task X"
  condition: ${{ not(skipTaskX) }}
- script: echo "Task Y"
  condition: ${{ not(skipTaskY) }}
